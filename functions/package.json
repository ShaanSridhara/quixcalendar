{
  "name": "quixcalendar-functions",
  "description": "Cloud Functions for Quix Calendar — Social panel post-publishing (Instagram/YouTube). Stats collection lives in ../social-scraper instead. Scaffold only: not deployed yet (Firebase project is on the Spark plan). See ../SOCIAL_SETUP.md.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "true",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  }
}
